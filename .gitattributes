# Only the paper PDFs stay in LFS. Images live in src/assets/ so Astro's image
# pipeline can optimize them, which requires real files rather than pointers.
public/assets/pdf/** filter=lfs diff=lfs merge=lfs -text
